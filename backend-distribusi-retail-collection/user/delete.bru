meta {
  name: Delete User
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/v1/users/{{user_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  Accept: application/json
}
