meta {
  name: Delete Company
  type: http
  seq: 5
}

delete {
  url: http://localhost:5050/api/v1/companies/{{companyId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  companyId: 
}
