meta {
  name: create
  type: http
  seq: 3
}

post {
  url: http://localhost:5050/api/v1/inventories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}


body:json {
  {
    "kode_barang": "BRG003",
    "nama_barang": "New Item from Bruno",
    "stok_barang": 150,
    "harga_barang": 25000,
    "min_stok": 15
  }
}
