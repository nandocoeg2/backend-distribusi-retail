meta {
  name: Delete Term of Payment
  type: http
  seq: 5
}

delete {
  url: http://localhost:5050/api/v1/term-of-payments/{{term_of_payment_id}}
  body: none
  auth: bearer
}

headers {
  accept: application/json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
