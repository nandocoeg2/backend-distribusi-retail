meta {
  name: login
  type: http
  seq: 2
}

post {
  url: http://localhost:5050/api/v1/auth/login
  body: json
  auth: none
}

headers {
  accept: application/json
}

body:json {
  {
    "email": "test123z@example.com",
    "password": "password123"
  }
}

script:post-response {
  bru.setEnvVar("access_token", res.body.user.accessToken);
}
