meta {
  name: update
  type: http
  seq: 4
}

put {
  url: http://localhost:5050/api/v1/invoice-penagihan/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "kepada": "Updated Customer",
    "total_discount": 75000,
    "kw": true,
    "fp": false
  }
}
